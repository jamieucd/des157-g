console.log('reading js')

// declare variable
var tri = "";

// declare loop
// i starts at 0, i wont loop greater than 7 times, increase by increments of one
for (var i=0; i < 7; i++) {
  tri += ('#');
  console.log(tri);
}
